# Known Bad Pattern: DATA_TYPE — SAM2

**Signal:** Refinery FLAGGED  **Date:** 2026-05-16

## What went wrong

At `WS-UPDATE-NUM`: `PIC S9(9)V99 COMP-3` was changed to `PIC S9(9)V99` (severity: MEDIUM).

At `TRAN-COUNT`: `PIC 9(5) COMP-3` was changed to `PIC 9(5)` (severity: MEDIUM).

## Pattern to avoid

Do NOT modify `WS-UPDATE-NUM` when optimising SAM2-style programs. The expression `PIC S9(9)V99 COMP-3` must be preserved exactly — changing it alters business logic.
